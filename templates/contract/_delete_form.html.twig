<form method="post" action="{{ path('app_contract_delete', {'id': contrat.id,'clientid':client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
